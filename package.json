{
  "name": "zenn-content",
  "version": "0.1.0",
  "private": true,
  "description": "Zenn へ公開する記事と本の原稿",
  "scripts": {
    "preview": "zenn preview",
    "new:article": "zenn new:article",
    "new:book": "zenn new:book",
    "lint": "textlint \"books/**/*.md\" \"articles/**/*.md\" README.md",
    "check": "npm run lint && npm run check:books",
    "check:books": "node scripts/check-books.mjs"
  },
  "devDependencies": {
    "textlint": "^15.2.2",
    "textlint-rule-preset-ja-technical-writing": "^12.0.2",
    "zenn-cli": "^0.5.2"
  }
}
